title: >-
  Test - basic workflow

description: >-
  Testing a simple workflow of two tools followed by two metrics.

tags:
  Test

database:
  url: sqlite:///./csvdb

setup:

  tools:
    - modify
    - revert

  metrics:
    - chars
    - lengths

input:

  data: *.txt

modify:
  options:

revert:
  options:

chars:
  reference_data: /proc/mounts

lengths:
  reference_data: /proc/mounts
